# fly.toml app configuration file generated for nupo-rag-server
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.

app = "nupo-rag-server"
primary_region = "ams"  # Amsterdam - close to Denmark

[build]

[deploy]
  release_command = "python -c 'import sys; print(\"Release command completed\")'"

[env]
  PORT = "8000"
  HOST = "0.0.0.0"
  PYTHONUNBUFFERED = "1"
  PYTHONDONTWRITEBYTECODE = "1"
  # Keep server active
  FLY_KEEP_ALIVE = "1"

[experimental]
  auto_rollback = true

[[services]]
  protocol = "tcp"
  internal_port = 8000
  processes = ["app"]

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [services.concurrency]
    type = "connections"
    hard_limit = 100
    soft_limit = 80

  [[services.tcp_checks]]
    interval = "30s"
    timeout = "10s"
    grace_period = "30s"

  [[services.http_checks]]
    interval = "30s"
    timeout = "10s"
    grace_period = "60s"
    method = "get"
    path = "/api/health"
    protocol = "http"
    tls_skip_verify = false

[[mounts]]
  source = "nupo_rag_storage"
  destination = "/app/persistent"
  initial_size = "10gb"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false  # Keep machines running
  auto_start_machines = true
  min_machines_running = 1    # Always keep 1 machine running
  max_machines_running = 3    # Scale up to 3 if needed

[http_service.concurrency]
  type = "requests"
  hard_limit = 200
  soft_limit = 150

# VM Configuration
[vm]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 2048

# Removed script checks - they were causing issues
